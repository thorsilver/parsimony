/* A short Laconic program dedicated to checking the conjecture that all perfect squares are less than 5. */

func square(x, y) {
    y = x * x;
    return;
}

func incr(x) {
    x = x + 1;
    return;
}

int a;
int b;

while (b < 5) {
    square(a, b);
    incr(a);
    
    print b;
}

halt;